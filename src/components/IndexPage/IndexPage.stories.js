import React from 'react';
import IndexPage from './IndexPage.react';
import { SB_LABELS } from '@constants';

export default {
  title: `${SB_LABELS.TEMPLATES}|IndexPage`,
};

export const Default = () => <IndexPage />;
