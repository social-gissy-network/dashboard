import { SB_LABELS } from '@constants';
import React from 'react';
import SelectedNodes from './SelectedNodes.react';

export default {
  title: `${SB_LABELS.MOLECULES}Node Info`,
};

export const Default = () => <SelectedNodes />;
