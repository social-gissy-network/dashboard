import React from 'react';
import { SB_LABELS } from '@constants';
import Loading from './Loading.react';

export default {
  title: `${SB_LABELS.MOLECULES}Loading`,
};

export const Default = () => <Loading />;
