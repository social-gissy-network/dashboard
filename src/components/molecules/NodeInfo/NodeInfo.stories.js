import React from 'react';
import { SB_LABELS } from '@constants';
import NodeInfo from './NodeInfo.react';

export default {
  title: `${SB_LABELS.MOLECULES}Node Info`,
};

export const Default = () => <NodeInfo />;
