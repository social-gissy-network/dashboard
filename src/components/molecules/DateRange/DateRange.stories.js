import React from 'react';
import { SB_LABELS } from '@constants';
import DateRange from './DateRange.react';

export default {
  title: `${SB_LABELS.MOLECULES}Date Range`,
};

export const Default = () => <DateRange />;
