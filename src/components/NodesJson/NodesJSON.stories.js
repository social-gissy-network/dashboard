import React from 'react';
import NodesJSON from './NodesJSON.react';
import { SB_LABELS } from '@constants';

export default {
  title: `${SB_LABELS.ATOMS}|NodesJSON`,
};

export const Default = () => <NodesJSON />;
