import { SB_LABELS } from '@constants';
import React from 'react';
import IconSelectArrow from './SelectArrow.svg.react';

export default {
  title: `${SB_LABELS.ICONS}/Select Arrow`,
};

export const Default = () => <IconSelectArrow />;
