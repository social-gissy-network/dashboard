import React from 'react';
import { SB_LABELS } from '@constants';
import Card from './Card.react';

export default {
  title: `${SB_LABELS.ATOMS}Card`,
};

export const Default = () => <Card>Some Text</Card>;
