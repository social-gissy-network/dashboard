import React from 'react';
import { SB_LABELS } from '@constants';
import NetworkGraph from './NetworkGraph.react';

export default {
  title: `${SB_LABELS.ATOMS}NetworkGraph`,
};

export const Default = () => <NetworkGraph />;
